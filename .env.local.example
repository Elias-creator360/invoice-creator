# Supabase Configuration
# Get these from your Supabase project dashboard: Settings -> API

# Public anon key - safe to expose in browser
# This key is used by the frontend (Next.js) for client-side operations
# It respects Row Level Security (RLS) policies
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...your-anon-key

# Service role key - NEVER expose in browser, only for backend!
# This key is used ONLY by the backend server (Express) for server-side operations
# It bypasses Row Level Security (RLS) and has full database access
# WARNING: Keep this secret and never commit it to version control
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...your-service-role-key

# Backend Configuration
PORT=3001
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
API_URL=http://localhost:3001
